# 座席配置システム

> 教室の席替えを楽しいイベントに変える、ブラウザで動く席替えアプリ

スロットマシン形式の抽選、運命の再会・地獄のシャッフル・ラストワンチャレンジなどの特殊演出、Web Audio APIによる効果音、印刷機能まで、席替え当日の盛り上がりに必要なものを一通り揃えています。

単一のHTMLファイルとJavaScriptで動作するため、サーバー不要。GitHub Pagesに置くだけで全クラスからアクセス可能。

---

## 特徴

- **抽選演出**: 名前・列・行の3スロットが順番に止まるドラマ仕立て
- **特殊イベント**: 確率で発動する5種類のイベント
  - 前後列確定演出（教卓前/最後列）
  - 窓際席確定演出（A列）
  - 交換イベント（席が決まった生徒と既存の誰かが入れ替わる）
  - 運命の再会（過去に隣だった生徒同士が再び隣に）
  - 地獄のシャッフル（既に決まった全員の席がランダムに入れ替わる）
- **ラストワンチャレンジ**: 最後の1人になったら座席ルーレットで誰かの席を奪い取る
- **席替え占い**: 席が決まったら100種以上のメッセージから運勢が表示される（カスタム可能）
- **効果音**: Web Audio APIで合成。外部ファイル不要、ON/OFF・音量調整可能
- **男女別座席指定**: 特定の席を男子席/女子席に指定可能、自動チェック付き
- **印刷機能**: B5横向き対応、座席表を生徒数に応じて自動レイアウト
- **データ保存**: localStorageで自動保存、JSONエクスポート/インポートで他PCに移送可能
- **ドラッグ&ドロップ**: 確定後の手動入れ替えにも対応（性別制限チェック付き）

## デモ

GitHub Pagesでホスティング後、`https://<username>.github.io/<repo-name>/` でアクセス可能。

## 使い方

### 初回セットアップ

1. 「名簿設定」をクリック
2. テキストエリアに生徒情報を入力
   - 形式: `番号[Tab]氏名[Tab]性別[Tab]フリガナ`
   - 番号・性別・フリガナは省略可能
   - Excelの該当列をコピペすればOK
3. 「保存」をクリック

### 席替え実行

1. 必要に応じて「座席設定」で使わない席を無効化
2. 「男女設定」で男子席/女子席を指定（必要時のみ）
3. 「カラー設定」で見た目をカスタマイズ
4. 「特殊演出設定」で各イベントの発生確率を調整
5. 「サウンド設定」で効果音をON/OFF
6. **「スタート」をクリック！**
7. 全員の席が決まったら印刷

### よく使う機能

- **教員視点**: 黒板側から見た配置に切り替え（教卓が下に来る）
- **保存/読み込み**: 設定をJSONでエクスポート、別のPCで読み込み可能
- **印刷**: B5横向き、未使用席は自動で省略可能
- **データ全削除**: 全localStorage初期化（注意）

## 技術構成

```
index.html         メインページ（全モーダル含む）
styles.css         全体スタイル（CSS変数で色・サイズを集約）
sound.js           Web Audio APIによる効果音合成
fortune.js         席替え占いメッセージのデフォルトセット
slot.js            スロットマシン本体・特殊演出
print.js           印刷プレビュー・印刷機能
script.js          メインロジック・座席管理・データ永続化
manifest.json      PWAマニフェスト(アプリ名・アイコン・テーマ)
service-worker.js  オフライン対応のキャッシュ管理
icon.svg           アプリアイコン
```

依存ライブラリなし。すべてVanilla JavaScript。

### 設計上の工夫

- グリッドサイズは `script.js` の `ROWS=7, COLS=8` 定数で一元管理。教室サイズが変わっても1行修正で対応
- 旧データ形式を自動マイグレーション（7列→8列、ブール値→オブジェクト形式）
- レスポンシブ対応：教室PC・タブレットでも崩れない
- 印刷専用CSSとプレビュー用CSSを分離

## デプロイ手順（GitHub Pages）

職場のファイルサーバーにJSが上げられない場合、GitHub Pagesでホスティングするのが最も簡単。

```bash
# 1. リポジトリ作成後、ローカルにクローン
git clone https://github.com/<username>/<repo-name>.git
cd <repo-name>

# 2. ファイルをコピーしてpush
cp -r /path/to/seat-app/* .
git add .
git commit -m "Initial commit"
git push origin main
```

その後GitHubのリポジトリページで:

1. **Settings** → **Pages**
2. **Source**: `Deploy from a branch`
3. **Branch**: `main` / `(root)`
4. **Save** をクリック
5. 数分待つと `https://<username>.github.io/<repo-name>/` でアクセス可能に

教室PCのブラウザにこのURLをブックマークしておけば、毎回そこからアクセスできます。

## アプリとしてインストールする(PWA対応)

このアプリはPWA (Progressive Web App) として動作するため、ブラウザから「インストール」してデスクトップアプリのように使えます。インストールすると：

- **オフライン動作**: ネットがなくても起動できる(席替え当日にネットが落ちても安心)
- **デスクトップアイコン化**: ブラウザを立ち上げる必要なし
- **独立ウィンドウ**: アドレスバーが消えて本物のアプリのよう
- **localStorage は維持**: 名簿・座席設定・履歴はそのまま使える

### インストール方法

GitHub Pagesに公開した後、`https://<username>.github.io/<repo-name>/` にアクセスすると、ブラウザがインストールを提案します。

**Windows / Mac (Chrome / Edge)**:
- アドレスバー右端に出る「インストール」アイコン(⊕またはモニターのアイコン)をクリック
- メニューから「インストール」 / 「アプリとしてインストール」

**iPhone / iPad (Safari)**:
- 共有ボタン → 「ホーム画面に追加」

**Android (Chrome)**:
- メニュー(︙) → 「アプリをインストール」 / 「ホーム画面に追加」

インストール後はスタートメニュー / アプリ一覧 / ホーム画面から起動可能。

### アプリの更新

アプリのファイル(JS/CSS)を更新したときは、`service-worker.js` の `CACHE_VERSION` をインクリメント(例: `v2.5.0` → `v2.5.1`)してpush。次回起動時に新しいキャッシュが取得されます。

### プライベートリポジトリで使いたい場合

GitHub Free アカウントでも、プライベートリポジトリでGitHub Pagesを使うには Pro プラン（$4/月）が必要です。学校で公開したくない場合は：

- **Cloudflare Pages**（無料・プライベートリポジトリOK）
- **Netlify**（無料枠あり）
- **ローカル配置**: 教室PCに直接ファイル一式を置いて `index.html` を開くだけでも動作

## カスタマイズ

### グリッドサイズの変更

`script.js` の冒頭：

```javascript
const ROWS = 7;
const COLS = 8;
```

ここを変えるだけで全体が追従。`styles.css` の `.seating-grid` の `grid-template-columns: repeat(8, ...)` だけは別途変更が必要（CSS側からは定数を参照できないため）。

### 占いメッセージのカスタマイズ

「メッセージ設定」モーダルから「カスタム占いセット」を選択すると、JSONで独自のメッセージセットを読み込めます。形式は `fortune.js` 内の `defaultFortuneSet` を参考に。

### 効果音の変更

`sound.js` の各メソッド（`winChime`, `frontBackChord`, etc.）の周波数や持続時間を変えるだけで音色が変わります。Web Audio APIなので外部音源不要。

### 演出の追加

特殊演出は `slot.js` の `SpecialEffectManager` クラスや `ShuffleEvent` クラスを参考に拡張可能。確率設定は localStorage で管理されているので、新しいスライダーをHTMLに追加して連動させればOK。

## ライセンス

MIT License - 詳細は [LICENSE](LICENSE) を参照

教員の方は自由に改造して使ってください。改善案やバグ報告は歓迎します。

## 変更履歴

[CHANGELOG.md](CHANGELOG.md) を参照
